[package]
name = "conquer-util"
version = "0.1.0"
authors = ["Oliver Giersch <oliver.giersch@b-tu.de>"]
edition = "2018"

[features]
default = ["std"]

align   = []
backoff = []
counter = ["std", "align"]
std     = []

[[bench]]
name = "backoff"
required-features = ["backoff"]
